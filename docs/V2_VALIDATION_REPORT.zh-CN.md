# V2 代码交付验证报告

日期：2026-09-22。最终运行代码审查点：`224f864858c7655fd65d8775c0086519b0512934`。
随后为避免覆盖主分支既有修复，`1e94c22` 将 `master` 的 `5c3709f` 队列/布局/HTTPS 修复合入开发分支，解决三个冲突。核心推荐模块未变；另将 V2 跳转保持为相对 URL。合并后的增量验收见下方。

## 验证结果

| 环境 | 结果 | 范围 |
|---|---|---|
| Windows Python 3.10.0 | 25 passed | 合同、迁移、模型、并发、Emby 假服务、Web、仓库 DB 副本 |
| GitHub Ubuntu Python 3.10.16 | 25 passed | 同上，JUnit 结果已上传 Actions artifact |
| Python 3.10.16 隔离 Docker | 23 passed, 2 skipped | 无网络运行；镜像不含两份历史 DB 文件，故这两项跳过 |
| 原 Python 3.7.4 / NumPy 1.17.1 / scikit-learn 0.21.3 Docker | 23 passed, 2 skipped | 原运行时模型兼容性；同样不含两份历史 DB 文件 |

[GitHub CI 完整日志](https://github.com/PanVivi/bustag/actions/runs/35720385218)。
两个 Docker 任务仅构建和运行隔离测试镜像，未发布镜像、未连接 NAS、未部署。

覆盖：

- 人工喜欢演员的低分/不匹配题材作品仍优先；单片不喜欢不改变演员状态。
- 多演员冲突进入待确认，包括已收藏作品；合并演员后旧身份操作仍有效。
- 多来源同名默认不合并，人工同义映射去重、标签排除及元数据刷新后旧评分失效。
- 重复媒体副本只形成一条弱兴趣信号，显式反馈作品不重复作为弱正例。
- Emby 离线保留快照、标过期、不错误删库；可恢复分页、增量 Etag、无 Etag 回退和同步互斥。
- 新片排除已拥有与显式不喜欢，本地推荐限制可播放快照，链接携带真实项目 ID 而非秘密。
- SQLite online backup、完整性检查、迁移失败回滚、旧字符串/整数外键兼容、原表及原文件校验不变。
- 同一数据拆分比较、最终全量重训、模型校验和/运行环境检查、上一代回滚。
- 标签版本发布竞态、旧评分任务覆盖新模型、并发映射版本丢更新均有防护与回归测试。
- Web 双入口/人工操作/CSRF；后台重复任务拒绝与过期租约恢复。

## 三次完整独立逻辑审查

三位独立审查者分别审阅数据与排序、集成与并发、整体合同/失败模式；发现问题后修正并复审。
全部最终复核 `224f864`，各自独立执行 25 项测试并给出无剩余实质代码阻断结论。

| 审查者 | 发现并修复的主要问题 | 最终结果 |
|---|---|---|
| review_data | 最新反馈迁移、旧 KNN 绕过纠错、Emby 离线标识/分页恢复、小样本选模、合并演员旧 ID | 通过，25 passed |
| review_integration | 模型映射发布竞态、旧重评分覆盖、手工映射版本丢更新/合并偏好并发窗口 | 通过，25 passed |
| review_final | 已收藏演员冲突未进入 review、Emby 元数据变更后旧评分失效 | 通过，25 passed |

这些结论只针对代码和隔离测试，不替代生产、真实来源或个人模型效果验收。

## 数据与模型比较边界

仓库示例 `data/bus.db` 有 2,037 部影片，0 条人工反馈；`app/bus.db` 为 0 部影片的旧整数外键结构。
已在临时副本迁移并比较原表/原文件，未修改它们，未把它们当作 NAS 当前数据库。

`V2_SYNTHETIC_DIAGNOSTIC.json` 是 60 条人为构造、正负各 30 的流水线诊断，
包含旧 KNN、已有分支 V2、规范 KNN 和逻辑回归对照、特征/演员优先/收藏消融、时间段诊断和置信区间。
通过 `PYTHONPATH=. python tools/v2_acceptance_report.py` 可在临时目录重现；工具不接受用户数据库参数。
**这些数字不能解释为用户个人模型的准确率或真实推荐提升。**
真实匿名样本分布、用户确认喜欢率、实际误推/漏推案例、历史无泄漏验证及辅助权重启用仍待真实数据。

## 隐私、兼容与生产状态

- 新提交只有代码、测试和公开安全报告，没有新增用户数据库、Emby 路径/清单、视频、模型或凭据。
- `.gitignore` 排除私有 DB、WAL、模型、环境变量文件；旧仓库本来跟踪的两个示例数据库保持不变。
- 本轮没有 NAS SSH、真实 Emby 请求、媒体修改或部署。
- JavDB 实测 HTTP 403，因此未编造或启用真实字段抓取；见 `V2_SOURCE_EVIDENCE.zh-CN.md`。
- 收藏辅助计算和消融已具备，正式辅助权重为 0；真实验证后仍需补参数启用流程。
- 旧运行时隔离测试通过；完整生产镜像、爬虫、NAS 版本差异、备份恢复演练及真实双入口播放仍待后续验收。

后续部署执行者请从 `V2_DEPLOYMENT_HANDOFF.zh-CN.md` 继续。最终规格尚未完成全部生产验收，不能据此直接宣称可无条件上线。

## 主分支已有修复合并验收

合并提交：`1e94c22784c30d102cfe731e277b0acc655efaaa`。GitHub 已确认 PR 为 MERGEABLE；没有合并到主分支。
Windows 全部 **27 passed**（16.32 秒）。新增回归验证：

- 在临时配置/数据库中加载真实 `bustag.app.index` 的路由，检查 `/`、`/recommend`、`/model`、`/do-training` 转向 V2 时保留相对 Location，避免公网 HTTPS 端口丢失。
- 旧推荐模板保留单/双排布局、连续匹配分数、设置入口和 V2 导航。
- 主应用的 V2 页面、状态页、队列 JSON 及设置页仍能响应；测试未启动爬虫或网络服务。

合并后的 [GitHub CI](https://github.com/PanVivi/bustag/actions/runs/35739002186) 全部通过：Linux **27 passed**，两个 Docker 环境各 **25 passed, 2 skipped**（仍为未打包的两份历史 DB）。
三位审查者均独立复查合并差异并重跑 **27 passed**，确认此前完整通过结论仍适用于 `1e94c22`；未发现合并新增实质代码阻断。
